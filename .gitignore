# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Next.js
.next/
next-env.d.ts
.vercel

# Env
.env
.env.local
.env.*.local

# Foundry (contracts/)
contracts/out/
contracts/cache/
contracts/broadcast/
# NOTE: contracts/lib/ is NOT ignored — dependencies (openzeppelin-contracts,
# forge-std) are tracked as pinned git submodules (see .gitmodules + foundry.lock).
# A fresh clone must run: git submodule update --init --recursive

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript incremental build info
*.tsbuildinfo
.env
apps/web/.env
contracts/.env
